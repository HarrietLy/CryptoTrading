spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: none #create
  sql:
    init:
      mode: never #always  # Options: always, embedded, never

third-party:
  binance:
    price_url: "https://api.binance.com/api/v3/ticker/bookTicker"
  huobi:
    price_url: "https://api.huobi.pro/market/tickers"